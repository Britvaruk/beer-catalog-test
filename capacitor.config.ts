import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'beer-catalog-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
